# =============================================================================
# Robot Framework E2E testing dependencies - SIRH onboarding agent
# Install with:  pip install -r requirements.txt
# =============================================================================
robotframework>=7.0
robotframework-seleniumlibrary>=6.3.0

# Optional but recommended:
# - webdriver-manager auto-downloads the matching browser driver so you don't
#   have to install chromedriver/geckodriver manually.
webdriver-manager>=4.0.0
